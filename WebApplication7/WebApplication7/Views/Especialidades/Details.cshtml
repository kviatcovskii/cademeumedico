@model IEnumerable<WebApplication7.Models.Especialidade>

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.especialidade1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.especialidade1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_especialidade }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_especialidade }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_especialidade })
        </td>
    </tr>
}

</table>
